"""Backward-compatibility — moved to preprocessing.splits."""

from aquacontam.preprocessing.splits import (  # noqa: F401
    assign_epa_region,
    geographic_split,
    random_split,
    split_summary,
)
