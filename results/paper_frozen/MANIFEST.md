# AquaContam frozen result snapshot

This directory holds the exact result JSONs that back the paper's tables, figures,
and reported numbers, version-controlled so reviewers can reproduce Table 2 et al.

- **Regime:** default-configuration, fixed seed 42, geographic split
  (train EPA regions 1,3,4,5,6 / val 2,7 / test 8,9,10).
- **Source run:** regenerated by `scripts/reproduce.py --all` (the maintainer's source
  result tree is not distributed; re-running the pipeline regenerates it).
- **Slimming:** `results.json` (and `results_random_split.json`, if present) have the
  large per-entry arrays ['y_true', 'y_prob', 'latitudes', 'longitudes', 'split_labels'] removed (~85 MB) -- the tables and
  Fig. 2 do not need them. The Extended-Data ROC/PR and national-risk-map figures
  need those arrays; regenerate them with `scripts/reproduce.py --all` if exact
  reproduction is required (the full prediction arrays are also included in the Zenodo
  archive).
- **Files:** 76 JSONs (each < 5.0 MB; guard enforced).
- **Integrity:** `checksums.sha256` lists SHA-256 of every JSON here.
- **Software:** see `software_versions.json` in this directory (its `aquacontam`
  dev-version string embeds a commit id from the private development history, which does
  not resolve in the public release copy).

## Provenance-free (environment-only) regime

`*_provenance_free.json` files come from the `scripts/reproduce.py --all
--provenance-free` run (a separate maintainer result tree, likewise not distributed) --
the honest environmental-signal
model. That run excludes exactly the 10 provenance/monitoring features in
`PROVENANCE_FREE_EXCLUDE` (`src/aquacontam/features/assembly.py`): `n_samples`,
`mean_detection_limit`, `population_served`, `log_population_served`, and the
six `*_nan` one-hot missingness indicators (data-source proxies). Hydrology
(`source_water_type_*`) and `system_type_*` values are retained. Rename map:

  - `results.json` -> `results_provenance_free.json`
  - `loro_cv.json` -> `loro_cv_provenance_free.json`
  - `multi_seed_stability.json` -> `multi_seed_stability_provenance_free.json`
  - `equity_analysis.json` -> `equity_analysis_provenance_free.json`
  - `bootstrap_ci.json` -> `bootstrap_ci_provenance_free.json`
  - `shap_T1.json` -> `shap_T1_provenance_free.json`

`results_provenance_free.json` is slimmed like `results.json`.

## Extra files

Result files produced outside the `--source` run directory, copied verbatim:

  - `mn_ascertainment.json` (from `results/mn_ascertainment.json`)

`t6_arsenic.json` is the T6 arsenic public-supply->domestic transfer result
(`scripts/reproduce.py --t6-arsenic`); T6 uses its own env-only NGA feature
cache and is unaffected by the provenance-free flag.

## Additive #55 updates (2026-07-11; maintainer-ratified, reproduction-gated)

Surgical `freeze_results.py --only` updates from the issue-#55 pre-publication
pass; every generator script reproduces a committed anchor before writing (see
`paper/DERIVATIONS.tsv` for input stamps):

  - `loro_cv_full.json` — gnn_gcn / gnn_sage / tabpfn completed to 10/10 LORO
    folds (`paper/compute_loro_gnn_tabpfn.py` + `paper/splice_loro_full.py`);
    the 11 previously-converged families are byte-verbatim.
  - `icp_diagnostics.json` — adds the T1 training `history` captured via the
    canonical `train_and_evaluate` path (`paper/compute_icp_history.py` +
    `paper/splice_icp_diagnostics.py`), reproduction-gated so the same run
    reproduces the frozen panel-b ICP T1 AUROC (0.7311); `results` block
    verbatim, `history_meta` records the truthful device and the gate.
  - `predictions_slim.json` — NEW: slim national T1/PFOS risk surface for the
    supplementary risk map (`paper/build_predictions_slim.py`).
  - `misgeocode_sensitivity.json` — NEW: wrong-ZIP geocoding flag counts and
    exclusion sensitivity for T1/T4 (`paper/compute_misgeocode_sensitivity.py`).

## Additive R7 updates (2026-07-12; maintainer-ratified, reproduction-gated)

Surgical updates from the R7 terminal pre-submission screen; each generator reproduces a
committed anchor before writing (see `paper/DERIVATIONS.tsv` for input stamps):

  - `source_adjusted_monitoring.json` — NEW: re-estimates the monitoring-intensity
    disparity with data-source fixed effects and a single-source (dominant-UCMR5) subset
    (`paper/compute_source_adjusted_monitoring.py`), reproduction-gated on the frozen
    `monitoring_inequity.json` POC ratio (1.847) and size-adjusted coef (0.060). Source-FE
    coef 0.043 (28% attenuation, p<1e-15); dominant-UCMR5 ratio 1.37 (coef 0.021, p<1e-3).
  - `power_analysis.json` — `loro_tests` block re-derived from the real
    `loro_cv.json` fold AUROCs (`paper/compute_loro_power.py`); the placeholder
    (mean_auroc 0.0/power 1.0) is replaced with the true mean AUROCs (T1 ~0.78, T4 ~0.68),
    `core_tests` and `ablation_tests` byte-verbatim. Root cause fixed in
    `src/aquacontam/pipeline/analysis/_evaluation.py`.

## ED Fig. 1 polygon-flag fix (2026-07-12; maintainer-ratified, reproduction-gated)

  - `misgeocode_sensitivity_v2.json` — NEW: wrong-ZIP mis-geocode flag counts and T1/T4
    exclusion sensitivity under the exact state-polygon rule
    (`paper/compute_misgeocode_sensitivity_v2.py`; `paper/_geo_flags.py` against
    `paper/assets/us_states_cb2023_5m.geojson`). Supersedes `misgeocode_sensitivity.json`
    for the ED Fig. 1 caption and Supplementary S7b; the v1 artifact is retained verbatim
    as the bounding-box-rule record (its loose overlapping first-match boxes falsely
    flagged ~64% of its 6,889 — correctly-located border systems — carving bands of
    dropped points along region borders in the split map). Dual reproduction gates: (A)
    unperturbed T1/T4 xgboost reproduce frozen `results.json` AUROC; (B) the retained
    legacy rule reproduces the v1 6,889/87,450 on the same coords frame. v2 flags 1,818;
    exclusion moves T1 0.864->0.866 and T4 0.699->0.697, inside the 0.02 band.

## Reviewer verification

```bash
# regenerate the tables from the frozen snapshot and confirm they match the committed ones
python paper/generate_tables.py --results results/paper_frozen --output /tmp/tables_check
python paper/verify_paper.py --results-dir results/paper_frozen   # hard-fails on any Table 2 mismatch
# verify file integrity
cd results/paper_frozen && sha256sum -c checksums.sha256
```
